[tox]  
requires =  
    tox>=4  
env_list =   
    py{308,309,310,311,312}  
  
[testenv]  
description = run the tests with pytest  
deps =  
    -r requirements-dev.txt  
commands =   
    pytest {posargs:tests}